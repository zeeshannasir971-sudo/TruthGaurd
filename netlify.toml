[build]
  # Basic build settings
  base = "frontend"
  publish = ".next"
  command = "npm run build"

[build.environment]
  PYTHON_VERSION = "3.11"
  # Ensure pip uses a compatible version
  PIP_DEFAULT_TIMEOUT = "100"
 
 [[plugins]]
   package = "@netlify/plugin-nextjs"
